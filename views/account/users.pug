extends ../layout

block content
  .row
    .col-sm-12   
    table.regTable
      .regHeading Users registered  
      tr
        th User email
        th Registered date
      each user in users
        tr
          td #{user.email}
          td #{user.createdAt}
        